/* arrary vector   deque  staac heap      list     forward_list  map set unodrder_set unorder_map
allocator   contaners ContanersAdpters   interaors algorithms AlgorithmsAdpters*/


#include<iostream>
#include<list>
#include <algorithm>
#include <vector>
#include <array>
#include<forward_list>
#include <deque>
#include<stack>
#include<set>
#include <map>
#include<hash_set>
#include <string>
#include<unordered_set>
using namespace std;


namespace mydeque
{
    void fun()
    {
      deque<int> mydeque;
      cout<<mydeque.front()<<endl;

    }
}

int main()
{
    unordered_set<string >c ;
    _Rb_tree<int,int,_Identity<int>,less<int>> mytree;
    auto myitertree =  mytree.begin();
count
     cout<<sizeof (mytree)<<endl;
replace
    forward_list<int> C;
    vector<int> myvec;

    array<int,100> myarray;

    deque<int> mydeque;
    stack<int> mystack;
//    map<int ,string> mymap;
//    cout<<sizeof (mylist)<<endl;
//    cout<<sizeof (myvec)<<endl;
//    initializer_list<int> a = {10,1\1};
}
